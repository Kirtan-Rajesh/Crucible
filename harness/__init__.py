"""Crucible — an authoring, reward, and calibration harness for runnable
security tasks used as SFT/RL training data.

The harness is task-agnostic. A *task* is a directory with a `task.yaml`
manifest, a rubric, a reference solver, a stochastic agent policy, and a runnable
environment. See docs/contract.md.
"""
__version__ = "0.1.0"
