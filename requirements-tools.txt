# Dependencies for the Crucible tooling that runs outside the containers:
# the reference solver, the grader, and the calibration harness.
# (Each challenge service pins its own deps under tasks/*/services/*/requirements.txt.)
requests==2.32.3
PyYAML==6.0.2
