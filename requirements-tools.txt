# Dependencies for the *tooling* that runs outside the containers:
# the reference solution, the grader, and the calibration harness.
# (The challenge services pin their own deps in challenge/*/requirements.txt.)
requests==2.32.3
PyJWT==2.9.0
PyYAML==6.0.2
